#deployment:
#  dockerhub:
#    branch: master
#    commands:
#      - $DOCKER_HUB_TRIGGER

dependencies:
  override:
    - sudo pip install -U docker-compose

test:
  pre:
    - docker-compose up -d
  override:
    - cd node/src; mocha